<!-- <div
  id="cookie-banner"
  class="fixed bottom-0 left-0 w-full bg-gray-200 px-4 py-2 text-center text-sm"
>
  <span class="inline-block align-middle mr-2"
    >Utilizamos cookies para mejorar tu experiencia en nuestro sitio web. Al
    continuar navegando, aceptas el uso de cookies no esenciales. <a
      href="/politica-cookies"
      class="underline">Más información</a
    ></span
  >
  <button
    id="cookie-accept"
    class="inline-block align-middle px-4 py-2 bg-green-500 text-white font-semibold border-none rounded cursor-pointer"
    >Aceptar</button
  >
</div>

<script is:inline>
document.addEventListener("DOMContentLoaded", function() {
  var banner = document.getElementById("cookie-banner");
  var acceptButton = document.getElementById("cookie-accept");

  acceptButton.addEventListener("click", function() {
    banner.style.display = "none";
    localStorage.setItem("cookieBannerAccepted", true);
  });

  var isBannerAccepted = localStorage.getItem("cookieBannerAccepted");
  if (isBannerAccepted) {
    banner.style.display = "none";
  }
});
</script> -->
